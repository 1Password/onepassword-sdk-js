Adds OTP support.
Brings improvements to the SDKs:
   Changes the structure of creating an item to use the new `ItemCreateParams`.
   Changes the name of `Update` to `Put` to better capture its functionality.
   Includes other bug fixes, such as:
      Fixing a bug with the `Secrets.Resolve` function, which was unable to resolve secret references containing periods.
